<div class="jumbotron {{ $classes ?? null }}">
    <div class="container">
        <h1 class="{{ $titleClass ?? 'display-4' }}">{{ $title ?? null }}</h1>
        <p class="lead">{{ $lead ?? null }}</p>
    </div>
</div>